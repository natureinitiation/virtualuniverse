    Запрос выполнен

Based on your custom instruction, I will be precise and concise.

📝 Использование audio_download.py

Это скрипт на Python, который конвертирует видеофайл в аудиофайл формата MP3, используя внешнюю утилиту FFmpeg.

Что нужно для работы:

    Установленный Python (для запуска скрипта).

    Установленная утилита FFmpeg (скрипт ее вызывает для конвертации). FFmpeg должен быть доступен из командной строки (прописан в переменной PATH).

Порядок действий:

    Сохраните этот код как файл, например, audio_download.py.

    Запустите скрипт из командной строки (терминала):
    Bash

python audio_download.py

Скрипт запросит:

Введите путь к видео:

Введите полный путь к вашему видеофайлу (например, /Users/user/Videos/my_video.mp4 или C:\Users\user\Videos\my_video.mp4) и нажмите Enter. Вы можете использовать символ ~ для обозначения домашней директории.

Скрипт сконвертирует видео.

Результат (MP3-файл) будет сохранен в той же папке, что и исходное видео, с тем же именем, но с расширением .mp3.
